# Snek - AI for the Snake Game

## Installation

This project requires the following dependencies:

```
libSDL2-dev
libSDL2-image-dev
```

Clone the repository and run the following commands:

```
./compile.sh
```

This script will place the built binary in the `bin/` directory.
